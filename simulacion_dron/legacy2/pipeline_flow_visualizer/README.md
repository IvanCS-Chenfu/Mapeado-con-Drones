# Snapshot legacy2 del visualizador pipeline_flow

Instantanea creada el 2026-08-10 al ampliar la subfase 3B.

Contiene el bridge ROS->SSE, la aplicacion web completa, el launch de
simulacion y los metadatos de paquete existentes antes de construir la base
visual incremental.

Este arbol es solo de consulta. No se instala ni se ejecuta. La version activa
vive en `simulacion_dron/src/visualizer/` y
`simulacion_dron/web/pipeline_flow/`.
